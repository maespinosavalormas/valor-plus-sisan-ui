import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

@NgModule({
  declarations: [
    // Shell components will be declared here
  ],
  imports: [CommonModule],
  exports: [
    // Export shell components
  ],
})
export class ShellModule {}
